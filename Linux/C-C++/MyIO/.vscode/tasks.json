{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            //"command": "gcc ${file} -o ${workspaceFolder}/${fileBasenameNoExtension}.out -g",
            "command":"gcc",
            "args": [
                "${fileDirname}/Main.c",
                "${fileDirname}/Print.c",
                "${fileDirname}/Scanf.c",
                "-o",
                "${workspaceFolder}/${fileBasenameNoExtension}.out",
                "-g"
            ]
        }
    ]
}